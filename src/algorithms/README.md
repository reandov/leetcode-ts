# Algorithms

Reusable algorithm implementations live here. Each algorithm should keep its
implementation, tests, and local notes together:

```text
src/algorithms/<algorithm-slug>/
|-- <algorithm-slug>.ts
|-- <algorithm-slug>.spec.ts
`-- README.md
```

Create a new algorithm scaffold with:

```bash
npm run new-algorithm -- "Algorithm Name"
```
