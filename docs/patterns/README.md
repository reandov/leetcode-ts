# Patterns

Problem-solving pattern notes live here. Use this area for recurring approaches
such as sliding window, two pointers, prefix sums, BFS, DFS, binary search,
dynamic programming, greedy choices, heaps, tries, and union-find.
